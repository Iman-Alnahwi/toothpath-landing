# pnpm 10+ blocks package build scripts unless they are named. esbuild ships a
# platform binary it needs to unpack; vue-demi picks its Vue 2/3 entry point.
only-built-dependencies[]=esbuild
only-built-dependencies[]=vue-demi
